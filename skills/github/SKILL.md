---
name: github
description: GitHub operations via gh CLI. Default repo context: xvadur/singularitas unless user specifies otherwise.
---

# GitHub

Default repo:
- `xvadur/singularitas`

Use `gh` for PR/issue/run workflows and always confirm repo before mutating actions.
