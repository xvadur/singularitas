## n8n access + workflow cleanup
- Confirmed live access to n8n instance at `https://xvadur.app.n8n.cloud` via public API token provided in chat.
- User requested keeping only workflow `Recepcia_Krau`; all other workflows were removed, leaving only `hBC48wiVo4h4QKNJ` (`Recepcia_Krau`).
- `Recepcia_Krau` remained `active=false`, `isArchived=false` after cleanup.

## Mail Engine direction approved by Adam
- Adam wants a Mail Engine in n8n for inbox triage: classify/label incoming mail, create summaries, log mails to Google Sheets, track subscriptions/payments/invoices, add renewal reminders to Calendar, and create draft replies for important mail.
- Safe operating preference: draft-only for replies at first; no auto-send.
- Preferred storage/output surfaces for V1: Gmail labels, Google Sheets, Google Calendar.

## n8n credential findings relevant to Mail Engine
- Confirmed credentials exist in n8n for:
  - Gmail: `gmail_recepcia`
  - Google Sheets: `Google Sheets account 2`
  - Google Calendar: `Google Calendar account` and `calendar_recepcia`
  - OpenRouter: `OpenRouter account`
- Probe result: OpenRouter credential works for chat-completion calls from n8n HTTP Request nodes.
- Probe result: `OpenAi account 4` credential failed for chat-completion calls with invalid API key, so OpenRouter is the safer AI path for Mail Engine classification/summarization right now.

## Mail Engine implementation state at compaction
- Began implementing `mail_engine_setup_v1` in n8n to bootstrap the Google Sheet (`Mail Engine V1`) and required Gmail labels (`ai/*`).
- Iterated on setup workflow after Google Sheets range-width errors in header initialization.
- Latest setup workflow ID at compaction: `9Mxwy5JP8QxHYxGy`.
- Execution verification was still in progress at compaction time; next step is to verify the latest setup run completes successfully, capture the created spreadsheet ID, then build the main polling/triage workflow on top of that setup output.
