## Strategy and build direction

- Preferred stack is now Obsidian for source-of-truth docs plus OpenClaw + Telegram + ACP for orchestration and execution.
- Strategic urgency was stated clearly: the window to build useful agent systems is now, and Xvadur's edge should be speed and execution rather than more meta-architecture.
- Existing support-heavy e-commerce artifacts for autodiely / pneuservis were audited and confirmed to already include:
  - lead shortlists and enriched datasets
  - personalized outreach drafts
  - an e-commerce support one-pager / Astro demo asset
  - Vapi + n8n e-shop support agent skeleton
- Core conclusion from the audit: the autodiely/pneuservis system is not missing content; it is missing one canonical OpenClaw-native orchestration spine.
- Decision: turn the existing autodiely / pneuservis / support-heavy e-commerce assets into a repeatable `Xvadur E-commerce Outbound Engine v1` instead of inventing a new system.
- The execution plan for that engine was written to Obsidian at:
  - `Operácie/Xvadur - E-commerce Outbound Engine/01 Plán/Xvadur - E-commerce Outbound Engine v1 - Plan.md`
- Highest-leverage next move after planning: write the engine spec and then prepare the first founder-ready batch from existing artifacts.

## Legacy cleanup follow-through

- Re-verified active state after teardown because earlier memory and reality had diverged.
- Confirmed there are no live references left in active OpenClaw config or non-memory workspace files.
- Removed remaining stale residue by moving broken Codex symlinks and stale Claude CLI cache entries to Trash.
- Removed the stale Obsidian link from `singularitas_opus/Operácie/README.md`.
- Cleaned the new outbound-engine plan so it no longer names the retired layer directly.
- Final remaining source-checkout residue was fully removed from the local `openclaw-main` export chain.
- Final verification: the only remaining literal `paperclip` hits are generic attachment icon names inside OpenClaw UI code, not the retired task/runtime layer.

## Karol voice-agent POC

- ElevenLabs agent `agent_8401k957p857f6fbpvf6bpj1s8h6` was repurposed into `agent Karol` as a Slovak conversation-first welfare check-in agent for Karol.
- New first message now frames the call transparently as Adam's virtual assistant and opens with a short human check-in.
- Added ElevenLabs webhook tool `report_karol_checkin` pointing to `https://xvadur.app.n8n.cloud/webhook/karol/checkin-report`.
- Saved local implementation artifacts for reproducibility:
  - `tmp/karol-agent-patch.json`
  - `tmp/n8n-karol-checkin-report.json`
- n8n workflow was not deployed because this runtime session did not have `N8N_BASE_URL` / `N8N_API_KEY` and the n8n UI required sign-in.
- Later the same day, the live ElevenLabs prompt and tool schema were expanded with a grocery lane:
  - fixed 30 A/B grocery pairs grouped by food type
  - new report fields: `grocery_selected_items`, `grocery_summary`, `grocery_completed`
- Verification confirmed the ElevenLabs agent/tool update succeeded, but `POST https://xvadur.app.n8n.cloud/webhook/karol/checkin-report` still returns `404`, so the production n8n workflow is still not active.
