# n8n Setup Checklist

## Goal

Reach a clean first-operator posture for `n8n`.

## Checklist

- verify `N8N_BASE_URL`
- verify `N8N_API_KEY`
- confirm instance access
- list existing workflows if available
- define naming convention
- define first workflow or verification target
- define validation evidence format
- define fallback if live setup is blocked

## Output

Tomorrow should end with:
- confirmed access or explicit blocker
- one canonical naming rule
- one validated first workflow target
- one manual fallback packet if live setup is not possible
