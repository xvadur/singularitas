# Status

- owner: `main`
- state: `active`
- category: `systems`

## Current result

- target runtime tree created
- doctrine rewrite completed
- legacy `studio` and `docs/voice` content extracted into new destinations
- imported playbooks still need selective cleanup where they mention pre-cutover paths
