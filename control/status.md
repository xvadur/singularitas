# Status

- Runtime: `jarvis-runtime-2026`
- State: `active`
- Mode: `workspace-first`, `cockpit-first`, `personal + business split`
- Canonical truth: `singularitas`

## Today

- founder entrypoint moved from `control` to `control/`
- `singularitas` now owns tasks, roadmaps, habits, CRM state, and daily founder control
- `firma` is legacy input only
- voice and integration material are being rehomed into `business/`, `docs/`, `systems/`, and `outputs/`

## Current focus

1. stabilize the new control surfaces
2. populate mail, CRM, and project objects into their new canonical homes
3. define the Paperclip handoff model on top of the new runtime
