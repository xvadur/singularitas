# 2026-03-12

- Adam confirmed that Google Places work should use the `goplaces` skill by default.
- Verified `goplaces` works with the newer backend/server-side Google Places API key stored in Obsidian note `google_places_creds.md`.
- Old runtime key is referrer-restricted and should not be relied on for CLI use.
