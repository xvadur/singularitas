
- Durable workspace decision: Adam wants the workspace significantly simplified and less fragmented; current direction is toward a very lean runtime structure optimized for OpenClaw compatibility and human legibility, with fewer top-level categories and less nested doctrine/reference taxonomy.
- Durable structure preference: Adam rejected `work/relationships` as an unnatural bucket; personal admin should live under `work/admin`, while CRM belongs under `work/revenue`.
- Durable design preference: Adam dislikes overly abstract folder categories such as separate `docs/architecture`, `docs/doctrine`, and `docs/schemas`; he prefers obvious, minimal, self-explanatory runtime surfaces over “AI OS taxonomy.”
- Active restructuring direction under discussion: converge toward a simple founder runtime with small number of clear surfaces (centered on `control`, `work`, `projects`, plus minimal support layers), while keeping root OpenClaw bootstrap files intact and aligning with OpenClaw docs.
