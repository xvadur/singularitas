- Implemented draft n8n backend artifacts for Marek/Jakub real-estate voice-agent webhooks in a single-canvas design. Artifacts live in `outputs/marek-n8n-backend-v1/`, especially `workflow_marek_realitky_tools_v1.json`, `canvas_design.md`, `google_sheets_schema.md`, `google_calendar_event_template.json`, and `implementation_notes.md`.
- Workflow covers 5 live ElevenLabs webhook tools: `log_lead_progress`, `check_availability`, `create_appointment`, `handoff_urgent`, `finalize_call`.
- Added mock/real runtime toggle via env var `MAREK_N8N_MODE`; mock mode returns deterministic responses, real mode uses Google Sheets + Google Calendar + availability/logistics logic.
- Encoded operating rules in workflow logic: callback 15m, viewing 60m, working hours 08:00-19:00, lunch block 12:00-13:00, weekends allowed, callbacks blocked during viewings, BA same-zone buffer 60m, BA cross-zone 90m, outside BA 120m, and up to 3 alternatives on unavailable slot.
- Updated runtime handoff `outputs/runtime-handoffs/2026-03-22-marek-elevenlabs-to-n8n.md` to reflect that schema/template/workflow draft now exist and remaining step is live import + credential binding + end-to-end webhook testing.
