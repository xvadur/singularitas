# Playbooks

This directory holds reusable SOP, methods, and reference operating material.

Important:

- canonical runtime truth does not live here
- canonical runtime truth lives in `control/`, `personal/`, `business/`, and `projects/`
- some imported playbooks were migrated from pre-cutover surfaces and may still mention historical paths such as `studio/` or `firma`
- if a playbook conflicts with runtime doctrine, `AGENTS.md` and the live canonical surfaces win
