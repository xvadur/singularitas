# MEMORY.md

Curated long-term memory for the main session.

Use this file for durable context worth carrying across days:
- important decisions
- stable preferences
- ongoing projects
- lessons worth keeping

Avoid secrets unless explicitly intended.

- Assistant identity in this workspace is `Jarvis`: the main operating agent, chief-of-staff layer, practical, direct, strategic, and disciplined.
- Adam is building Xvadur and prefers directness, speed, concrete thinking, and execution-ready outputs over generic startup theatre.
- Xvadur strategy shifted from a clinic/dental-first wedge to a broader voice-agent / AI operator offer for businesses that miss calls, lose leads, and fail at follow-up.
- The intended operating model is a narrow-agent team: Jarvis as chief-of-staff plus four specialist execution agents (`cso`, `cro`, `cmo`, `coo`).
- The preferred operating stack is Obsidian for source-of-truth docs plus OpenClaw + Telegram + ACP for orchestration and execution.
- The retired external task-layer experiment has been fully removed from active runtime/workspace paths; if `paperclip` still appears in future grep results inside `openclaw-main`, treat generic attachment-icon literals as unrelated UI symbols unless proven otherwise.
- The intended operating model still mirrors the current narrow-agent structure:
  - Jarvis = chief of staff / intake / routing
  - `cso` = strategy/research
  - `cro` = revenue/outreach
  - `cmo` = content/messaging
  - `coo` = build/operations
- One active build direction is to turn the existing autodiely / pneuservis / support-heavy e-commerce artifacts into a repeatable OpenClaw-native outbound engine rather than inventing a new GTM system from scratch.
- The real persistent runtime for the multi-agent setup should be Discord or Telegram, not webchat.
- The notes and operating workspace live under `/Users/_xvadur/singularitas`, with the active Obsidian knowledge home at `/Users/_xvadur/singularitas_opus`.
- Daily memory belongs in `memory/YYYY-MM-DD.md`; `MEMORY.md` should stay curated and contain only durable context.
- The main recurring operating risk is drift into meta-work, tooling, and invisible effort instead of visible shipped artifacts.
- A good operating law for this workspace: each meaningful day should produce at least one visible artifact in build, sales, content, research, or operations.
- System improvement is valuable only when it supports client delivery, revenue, product progress, or content extracted from real work.
- Google Workspace should be treated as one unified operating surface with two explicit accounts:
  - personal: `yksvadur.ja@gmail.com`
  - business: `adam@xvadur.com`
- Google Workspace migration direction is to keep skill UX stable while moving the backend toward `gws`.
- Personal and work Google auth must remain isolated:
  - `~/.config/gws` for `yksvadur.ja@gmail.com`
  - `~/.config/gws-xvadur` for `adam@xvadur.com`
- `gws` is already a viable core backend for Gmail, Calendar, and Drive, but not every Google API surface is enabled yet.