# 2026-03-18

## Runtime decisions

- Canonical operating model is now `two layers + two active business engines`.
- Personal Layer = `jarvis`, `personal-ops`, `personal-web`.
- Business Layer = `revenue`, `voice`, `integration`, `delivery`, `proof`, `web`.
- Primary 18-month revenue engine = phone-first deployments.
- Parallel engine = web/content/proof systems.
- Vendor/MCP/connectors stay opportunistic unless they open distribution or a concrete deal.

## Implementation notes

- Existing functional lanes (`research`, `growth`, `build`, `ops`, `janitor`) remain active as support execution lanes during transition.
- Legacy C-level workspaces remain migration sources only.
- Project template now expects `approvals/`, `validation/`, and `content/` alongside the existing spec/artifact/staging/handoff shape.
- Boundary reset is now explicit: `Obsidian -> singularitas -> firma`.
- `singularitas` now owns runtime doctrine, mission traces, packets, routing, and execution machinery only.
- Historical human-facing material was moved out of `singularitas/outputs`; runtime outputs now mean runtime evidence or runtime handoffs only.
- Config-level smoke test passed for OpenClaw route bridge and specialist workspace presence; live ingress and CRM remain separate checks.

## Jarvis identity pack upgrade

- Core Jarvis docs were rewritten to strengthen operator posture and make the system feel more like a live digital chief of staff than a generic assistant.
- Rewritten files: `SOUL.md`, `IDENTITY.md`, `USER.md`, `HEARTBEAT.md`, `AGENTS.md`, `MEMORY.md`, `TOOLS.md`.
- New behavioral direction: verdict-first, sharper frame control, less helper tone, less overexplaining, more operator presence.
- `HEARTBEAT.md` is now a real executive pulse spec with strict signal-only rules.
- `AGENTS.md` now acts as runtime doctrine with routing law, packet contract, approval model, and system-of-record discipline.
- `TOOLS.md` was reduced to a clean local cheat sheet instead of mixed doctrine.
- A reusable test prompt for validating the new Jarvis identity in a fresh conversation was created.

## Session continuation

- Jarvis identity pack passed an in-chat calibration test; preferred behavior confirmed as verdict-first, brief, frame-holding, low-fluff, and operator-like rather than generic-assistant-like.
- `HEARTBEAT.md` was tightened again into a shorter, stricter signal-only executive pulse spec with `HEARTBEAT_OK` as the exact no-signal response.
- A new project direction was defined in Obsidian: `Narrative Watchtower SK`, an OpenClaw-native briefing/intel engine for the Slovak dezinfo-political space, designed as signal -> brief -> content rhythm rather than a generic dashboard or feed.
- Obsidian scaffolds created for this project: `docs/plans/2026-03-19-narrative-watchtower-sk-phase-1.md`, `docs/plans/2026-03-19-narrative-watchtower-sk-master-spec.md`, and daily note `Denník/Denné/2026-03-19.md` in `singularitas_opus`.
- A new workspace skill was created: `skills/advanced-spec`, intended as the default spec-amplification layer for Adam's compressed high-intent ideas before `/plan` or `/ironman`.
- ClawHub scan surfaced likely-useful external skills to audit later, with strongest candidates including browser automation, scraping, Firecrawl search, YouTube watcher, and voice-agent related skills.

## Founder loop artifact

- Added `projects/founder-cockpit/validation/2026-03-18-jarvis-usability-checklist-v1.md` as the concrete end-to-end test contract for `usable Jarvis v1`.
- The checklist anchors eight gates: live ingress, startup contract, `capture/decide/execute` surface, promotion behavior, seed records, CRM briefing integration, feedback loop, and one canonical daily loop.
- It includes the first real Telegram session script plus the exact business/runtime surfaces that must be touched for a valid pass.

## Parallel usability pass

- Today's founder-cockpit checklist was split into six parallel subagent slices: ingress, startup contract, founder command surface, CRM/briefing integration, promotion behavior, and founder-day runbook.
- New artifacts created from that pass:
  - `projects/founder-cockpit/validation/2026-03-18-live-ingress-check.md`
  - `projects/founder-cockpit/spec/startup-contract.md`
  - `projects/founder-cockpit/spec/founder-command-surface.md`
  - `projects/founder-cockpit/validation/2026-03-18-crm-briefing-integration.md`
  - `projects/founder-cockpit/spec/promotion-rules.md`
  - `projects/founder-cockpit/validation/2026-03-18-founder-day-runbook.md`
  - `projects/founder-cockpit/validation/2026-03-18-parallel-workboard.md`
- The practical blockers after the pass are now narrow:
  - live Telegram ingress still needs a real founder-side message
  - CRM data exists locally, but founder briefing still needs direct DB-path wiring to replace `NO_SIGNAL`

## Workspace cleanup cutover

- Legacy C-level workspaces (`cso`, `cro`, `cmo`, `coo`) were moved out of the active runtime into `archive/cutover-2026-03-18/legacy-workspaces/`.
- Root `business/`, root `tmp/`, and old non-canonical `outputs/` subtrees were moved into `archive/cutover-2026-03-18/`.
- Legacy outbound-engine docs that pointed at archived paths were moved into `archive/cutover-2026-03-18/legacy-docs/projects/`.
- Active workspace root is now clean to the canonical structure: doctrine files, canonical `agents/`, `projects/`, `outputs/`, `docs/`, `data/`, `skills/`, `systems/`, `scripts/`, `memory/`, `archive/`.
- Active docs were updated so `docs/workspace-overview.md`, `docs/workspace-map.md`, `agents/REGISTRY.md`, `outputs/README.md`, and `docs/runtime-tools-map.md` now describe the post-cutover runtime instead of transition state.
- Git hygiene was tightened after the cleanup: root `.gitignore` was restored/updated, archived local runtime state was excluded, and the workspace was prepared for a single cutover commit instead of leaving the refactor as an uncommitted drift pile.
