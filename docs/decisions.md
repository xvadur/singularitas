# Decisions

## 2026-03-27
- Workspace simplified toward a smaller, more human-legible runtime.
- `personal/` and `business/` were merged into `work/`.
- `work/relationships/` was rejected; `work/admin/` and `work/revenue/` are the valid split.
- `skills/` remains top-level for OpenClaw compatibility.
- `systems/` is folded into `scripts/`.
- `docs/` is reduced to minimal runtime docs and runbooks.
