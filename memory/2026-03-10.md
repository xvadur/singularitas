# 2026-03-10

## Workspace and runtime

- Confirmed the secondary operational workspace/home is the Obsidian vault at `/Users/_xvadur/singularitas`.
- Set up the workspace as an AI-friendly notes system with `00_Inbox`, `10_Projects`, `20_Areas`, `30_Resources`, `40_System`, and `90_Archive`.
- Added inbox/category templates and an inbox routing helper script for Obsidian notes.
- Set the default Obsidian vault to `/Users/_xvadur/singularitas_opus` and migrated the workflow structure there.

## Identity and operating model

- Assistant identity for this workspace is `Jozef`.
- The intended operating model is one main Chief of Staff plus four specialist execution agents.
- The knowledge architecture lives in `/Users/_xvadur/singularitas_opus`; the agent framework is Chief of Staff + 4 execution agents.

## Gateway and media

- Voice replies were failing because Telegram media was written to plain `/tmp/*.mp3`; OpenClaw now expects allowed paths like `/tmp/openclaw` or state directories.
- Confirmed the rescue profile contains TTS/STT configuration and local transcription helper scripts.

## Filesystem notes

- Could not create a top-level `/_xvadur` symlink because the filesystem root was read-only in that runtime.
- Created fallback home-directory links instead:
  - `/Users/_xvadur/workspace-core-link -> /Users/_xvadur/xvadur_openclaw`
  - `/Users/_xvadur/workspace-rescue-link -> /Users/_xvadur/.openclaw-rescue`
