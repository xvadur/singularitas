# Personal Action Queue

- Date: `2026-03-24`
- Owner: `personal-ops`
- Basis: first verified live refresh
- Purpose: concrete founder-facing queue after state read

## Now

### 1. Fix OpenAI billing
Why it matters:
- Plus payment failed
- access is still active for now, but this can become tooling friction if left open

Suggested move:
- update the payment method first

### 2. Expandeco thread is cancelled
Decision:
- do not proceed with Expandeco collaboration
- remove it from the active personal queue

Suggested move:
- keep it out of the working queue unless you explicitly reopen it later

### 3. Decide the Legalia trademark thread
Why it matters:
- they are waiting for more detail about your activity to proceed with the `xvadur` trademark help
- unresolved legal/admin threads create slow-burn drag

Suggested move:
- either send the missing detail
- or park the thread deliberately instead of letting it float

## Cleanup next

### 4. Reminder source cleanup is done
Result:
- legacy `xvadur` duplicates were retired
- unique reminder `Spraviť demo na AI recepciu` was moved into `XVADUR`
- `XVADUR` is now the single trusted reminder surface

Suggested move:
- keep all future reminder work inside `XVADUR` only

### 5. Reconcile overdue follow-ups
Visible overdue items:
- `Spraviť demo na AI recepciu`
- `Call Bohuš Dentmax`
- `Call Semafor Dent - Ondrej Užovič`

Suggested move:
- for each item, choose one of:
  - do now
  - reschedule explicitly
  - kill as stale

## Not urgent right now

- no visible calendar collision today
- no visible meeting load in the next 48h
- unread social/newsletter mail exists, but it is not the real issue

## Dominant next move

If you want the personal layer to start feeling real, the best next move is:
- clear the OpenAI billing issue
- then resolve the Legalia thread
- then clean the reminder debt so the queue becomes trustworthy
