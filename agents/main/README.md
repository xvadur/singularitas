# Main Agent Notes

Use this folder for main-agent-specific long-form notes that should not live in the root bootstrap files.

Keep the root files as the canonical startup surface. Use this folder only for material that is clearly main-agent-only.
